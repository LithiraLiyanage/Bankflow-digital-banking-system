import {useState} from "react";import{useNavigate,Link}from"react-router-dom";import{toast}from"react-toastify";import{useAuth}from"../context/AuthContext";
const Login=()=>{const{login}=useAuth();const nav=useNavigate();const[f,setF]=useState({email:"",password:""});const submit=async e=>{e.preventDefault();try{const p=await login(f.email,f.password);toast.success("Login successful");nav(p)}catch(e){toast.error("Login failed")}};return <main className="flex justify-center px-4 py-16"><form onSubmit={submit} className="card p-8 w-full max-w-md"><h1 className="text-3xl font-black">Login</h1><input className="input mt-6" placeholder="Email" onChange={e=>setF({...f,email:e.target.value})}/><input className="input mt-4" type="password" placeholder="Password" onChange={e=>setF({...f,password:e.target.value})}/><button className="btn-primary mt-6 w-full">Login</button><p className="mt-4 text-center">No account? <Link className="text-primary font-bold" to="/register">Register</Link></p></form></main>};export default Login;
